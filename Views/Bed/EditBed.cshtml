@using Tracker.Models
@model List<Bed>

<h2>Edit Beds</h2>
<body style="background-color: #D1CA08 ">
    @if (Model.Count == 0)
    {
        <p>No beds selected for editing</p>
    }
    else
    {
        <form asp-controller="Bed" asp-action="EditBedSubmit" method="post" style="background-color:beige; border: 1px solid">
            @foreach (var bed in Model)
            {
                <div>
                    <h1>Bed: @bed.Name</h1>
                    <label>Name:</label>
                    <input type="text" name="beds[]" value="@bed.Name" />
                    <br />
                    <input type="hidden" name="bedIds[]" value="@bed.Id" />
                </div>
            }
            <br />
            <button type="submit" formaction="@Url.Action("EditBedSubmit", "Bed")" class="btn btn-light" style="border:1px solid">Save Changes</button>

        </form>
    }
</body>